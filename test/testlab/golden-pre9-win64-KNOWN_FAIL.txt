m1--intl wtou-CESU8: el archivo historico guarda los nombres fuera del BMP como CESU-8 (bug de wtou previo al arreglo). Los bytes estan en el archivo, no en el lector.
m2--intl wtou-CESU8: el archivo historico guarda los nombres fuera del BMP como CESU-8 (bug de wtou previo al arreglo). Los bytes estan en el archivo, no en el lector.
m3--intl wtou-CESU8: el archivo historico guarda los nombres fuera del BMP como CESU-8 (bug de wtou previo al arreglo). Los bytes estan en el archivo, no en el lector.
m5--intl wtou-CESU8: el archivo historico guarda los nombres fuera del BMP como CESU-8 (bug de wtou previo al arreglo). Los bytes estan en el archivo, no en el lector.
ma_brotli_11--intl wtou-CESU8: el archivo historico guarda los nombres fuera del BMP como CESU-8 (bug de wtou previo al arreglo). Los bytes estan en el archivo, no en el lector.
ma_brotli_1--intl wtou-CESU8: el archivo historico guarda los nombres fuera del BMP como CESU-8 (bug de wtou previo al arreglo). Los bytes estan en el archivo, no en el lector.
ma_brotli_5--intl wtou-CESU8: el archivo historico guarda los nombres fuera del BMP como CESU-8 (bug de wtou previo al arreglo). Los bytes estan en el archivo, no en el lector.
ma_bzip2_9--intl wtou-CESU8: el archivo historico guarda los nombres fuera del BMP como CESU-8 (bug de wtou previo al arreglo). Los bytes estan en el archivo, no en el lector.
ma_bzip2--intl wtou-CESU8: el archivo historico guarda los nombres fuera del BMP como CESU-8 (bug de wtou previo al arreglo). Los bytes estan en el archivo, no en el lector.
ma_lzfse_1--intl wtou-CESU8: el archivo historico guarda los nombres fuera del BMP como CESU-8 (bug de wtou previo al arreglo). Los bytes estan en el archivo, no en el lector.
ma_lzfse--intl wtou-CESU8: el archivo historico guarda los nombres fuera del BMP como CESU-8 (bug de wtou previo al arreglo). Los bytes estan en el archivo, no en el lector.
ma_ppmd_2--intl wtou-CESU8: el archivo historico guarda los nombres fuera del BMP como CESU-8 (bug de wtou previo al arreglo). Los bytes estan en el archivo, no en el lector.
ma_ppmd_32--intl wtou-CESU8: el archivo historico guarda los nombres fuera del BMP como CESU-8 (bug de wtou previo al arreglo). Los bytes estan en el archivo, no en el lector.
ma_ppmd_6--intl wtou-CESU8: el archivo historico guarda los nombres fuera del BMP como CESU-8 (bug de wtou previo al arreglo). Los bytes estan en el archivo, no en el lector.
ma_snappy--intl wtou-CESU8: el archivo historico guarda los nombres fuera del BMP como CESU-8 (bug de wtou previo al arreglo). Los bytes estan en el archivo, no en el lector.
store--intl wtou-CESU8: el archivo historico guarda los nombres fuera del BMP como CESU-8 (bug de wtou previo al arreglo). Los bytes estan en el archivo, no en el lector.
m1--mixed case-collision: solo falla con un lector de Windows (demo.PQL vs Demo.PQL). Ver case-collision-silent-loss.
m2--mixed case-collision: solo falla con un lector de Windows (demo.PQL vs Demo.PQL). Ver case-collision-silent-loss.
m3--mixed case-collision: solo falla con un lector de Windows (demo.PQL vs Demo.PQL). Ver case-collision-silent-loss.
m5--mixed case-collision: solo falla con un lector de Windows (demo.PQL vs Demo.PQL). Ver case-collision-silent-loss.
ma_brotli_11--mixed case-collision: solo falla con un lector de Windows (demo.PQL vs Demo.PQL). Ver case-collision-silent-loss.
ma_brotli_1--mixed case-collision: solo falla con un lector de Windows (demo.PQL vs Demo.PQL). Ver case-collision-silent-loss.
ma_brotli_5--mixed case-collision: solo falla con un lector de Windows (demo.PQL vs Demo.PQL). Ver case-collision-silent-loss.
ma_bzip2_9--mixed case-collision: solo falla con un lector de Windows (demo.PQL vs Demo.PQL). Ver case-collision-silent-loss.
ma_bzip2--mixed case-collision: solo falla con un lector de Windows (demo.PQL vs Demo.PQL). Ver case-collision-silent-loss.
ma_lzfse_1--mixed case-collision: solo falla con un lector de Windows (demo.PQL vs Demo.PQL). Ver case-collision-silent-loss.
ma_lzfse--mixed case-collision: solo falla con un lector de Windows (demo.PQL vs Demo.PQL). Ver case-collision-silent-loss.
ma_ppmd_2--mixed case-collision: solo falla con un lector de Windows (demo.PQL vs Demo.PQL). Ver case-collision-silent-loss.
ma_ppmd_32--mixed case-collision: solo falla con un lector de Windows (demo.PQL vs Demo.PQL). Ver case-collision-silent-loss.
ma_ppmd_6--mixed case-collision: solo falla con un lector de Windows (demo.PQL vs Demo.PQL). Ver case-collision-silent-loss.
ma_snappy--mixed case-collision: solo falla con un lector de Windows (demo.PQL vs Demo.PQL). Ver case-collision-silent-loss.
store--mixed case-collision: solo falla con un lector de Windows (demo.PQL vs Demo.PQL). Ver case-collision-silent-loss.
